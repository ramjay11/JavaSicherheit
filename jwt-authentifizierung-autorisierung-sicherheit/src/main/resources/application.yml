spring:
  datasource:
    url: jdbc:mysql://localhost:3306/jwt_authentifizierung_autorisierung_sicherheit_db
    username: root
    password: Saint123
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true
    properties:
      hibernate:
        format-sql: true
    database: mysql
    database-platform: org.hibernate.dialect.MySQL8Dialect
server:
  port: 8082